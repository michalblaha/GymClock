# GymClock 2.1.1 — Release Notes

## Heart rate
- Heart rate now gets measured the whole time the app is open. Fast HRM
  sampling (~1 s) runs on the idle screen, so your BPM reading is live the
  moment you launch the app — you no longer have to start a workout first.
  Sampling drops back to the OS default interval once you close the app.
